import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dzlife.algerianlifesimulator',
  appName: 'DZ LIFE',
  webDir: 'www',
  loggingBehavior: 'none',
  android: {
    backgroundColor: '#080a0c'
  }
};

export default config;
